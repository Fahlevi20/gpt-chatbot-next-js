import { Box, DarkMode } from "@chakra-ui/react";

export default function ChatScreen() {
    return(
        <Box backgroundColor={"blue"} width='100%' height={800} p={100}>
            this is chatscreen
        </Box>
    )
}